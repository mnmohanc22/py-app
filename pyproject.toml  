# pyproject.toml
[tool.pytest.ini_options]
testpaths    = ["tests"]
python_files = ["test_*.py"]

[tool.black]
line-length = 100

[tool.coverage.run]
source = ["app"]
omit   = ["tests/*", "venv/*"]


[tool.setuptools.dynamic]
version = { file = "VERSION" }